import { View, Text } from 'react-native';
import React from 'react';

export default function ResultNotFound(props) {
    const { search } = props;
  return (
    <View>
      <Text>No hay resultado de </Text>
    </View>
  );
}
